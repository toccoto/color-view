type ImagePalette {
  searchValue: String
  imageSrc: String
  randomWord: String
  palette: [Palette]
}

type Palette {
  colorType: String
  hex: String
}

type Query {
  imagePalette(searchValue: String): ImagePalette
}

